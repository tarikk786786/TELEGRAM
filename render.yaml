services:
  - type: web
    name: telegram-hub-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: API_ID
        sync: false
      - key: API_HASH
        sync: false
      - key: FRONTEND_URL
        value: https://*.vercel.app
